name: ♻ CI
on: 
  workflow_call:
  pull_request:
    branches: 
      - main 

permissions:
  contents: read

jobs:
  lint:
    name: 🔎 Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          cache: 'pip'
      - run: pip install pylint
      - run: pylint src
